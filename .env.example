# Model API key for the Reader/Linker agents (Rig). Not required for
# `cargo test -p airlock-core -p airlock-policy` — those crates never touch the network.
MODEL_API_KEY=

# Port the Axum API/SSE server listens on.
PORT=8080
